package lambdas.anonymousFunction;

public interface Prices {

    // Métodos de uma interface devem ser públicos e abstratos (não tem corpo e chaves)

    public abstract String media(double p1, double p2, double p3, double p4);
}
